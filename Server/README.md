## postar - server

### Docker setup

``` bash
    cd docker/
    docker-compose up -d
```

### Node build and start

```bash
    npm install
    npm start
```

### Optional parameters

```javascript
    // swich typeorm logging, in ormconfig.json
    "logging": false
```